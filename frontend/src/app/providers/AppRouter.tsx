import { BrowserRouter } from 'react-router-dom';
import { Routing } from '@/pages/Routing';

export const AppRouter = () => {
  return (
    <BrowserRouter>
      <Routing />
    </BrowserRouter>
  );
};
